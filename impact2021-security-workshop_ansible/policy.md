
The sample policies have been prepared for you. We learned about policy in the last module.

Run `cat conjur.yml`{{execute}} to review the root policy

Run `cat server.yml`{{execute}} to review the server policy

Run `cat ansible.yml`{{execute}} to review the ansible policy

